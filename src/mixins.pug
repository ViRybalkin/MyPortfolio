mixin icon(name, className)
  - var icon = require(`images/icons/${name}.svg`).default;
  svg(class=className viewBox=icon.viewBox preserveAspectRatio="none")
    use(xlink:href=`sprite.svg#${icon.id.replace('-usage', '')}`)

mixin image(path, className, alt,height,width)
  img(src=require(`images/${path}`).default class=className alt=alt height=height width=width)

- const title = "MyPortofolio"
-
  const socialItem ={
    'insta' : 'https://www.instagram.com/rybalkinvi',
    'github': 'https://github.com/ViRybalkin',
    'vk' : 'https://vk.com/kinjeks',
    'mail': 'mailto:kinjeks@gmail.com',
  };

-
  var userInfo = [
    ['Проживаю','Ярославль'],
    ['Возвраст','24 года'],
    ['Родился',"8 августа 1996"],
  ];

- 
  const menuItem = {
  'Обо мне': '#about',
  'Навыки': '#values',
  'Работы': '#works',
  'Отзывы': '#reviews',
  };

- 
  const menuItemMobile = {
  'Главная':'#main',
  'Обо мне': '#about',
  'Навыки': '#values',
  'Работы': '#works',
  'Отзывы': '#reviews',
  };

