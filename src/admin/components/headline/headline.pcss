.headline-component{
  background: $dark-blue;
  color: #fff;
}

.container{
  display: flex;
  padding: 20px 60px;
  align-items: center;
}

.btns{
  margin-left: auto;
}

.btn{
  color: rgba(#fff,.9);
  text-decoration: underline;
  cursor: pointer;
  appearance: none;
  border: none;
  background-color: transparent;
  font-size: 16px;
}

.title{
  margin-left: 30px;
  color: rgba(#fff,.5);
}